# Low BCrypt cost so Hegel properties that drive the real application through HTTP
# (many register/login iterations per run) stay fast. Production keeps BCrypt's default 10.
app.password.bcrypt-strength=4
